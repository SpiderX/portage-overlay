# Config file for /etc/init.d/btsync

# Various options.
# run `btsync --help` for valid cmdline options
#OPTS=""

# User and group daemon runs as
USER="btsync"
GROUP="btsync"

# Resilio Sync config file
CONFIG="--config /etc/btsync/btsync.conf"
