# Config file for /etc/init.d/gitlab-git-http-server

GITLAB_USER="git"

# The -listenXxx settings determine where gitlab-git-http-server
# listens for connections. To listen on localhost:8181, write
# '-listenNetwork tcp -listenAddr localhost:8181'.
# The -authBackend setting tells gitlab-git-http-server
# where it can reach Unicorn.
OPTS="-listenUmask 0 -listenNetwork unix -listenAddr /tmp/gitlab-git-http-server.socket -authBackend http://127.0.0.1:8080"

# Root for GitLab repositories
REPOSITORIES="/home/git/repositories"
