--- ../tests/005-uv_listen_cb-not-destroyed.phpt	2026-07-10 00:15:17.377332466 +0300
+++ ../tests/005-uv_listen_cb-not-destroyed.phpt	2026-07-10 00:17:49.367143174 +0300
@@ -4,6 +4,8 @@
 <?php
 class TcpServer
 {
+    private $accepted = 0;
+
     private $loop;
     private $tcp;
 
@@ -25,10 +27,12 @@
             $client = uv_tcp_init($this->loop);
             uv_accept($server, $client);
 
-            uv_read_start($client, function ($socket, $buffer) {
-                echo 'OK', PHP_EOL;
-                uv_close($socket);
-            });
+            echo 'OK', PHP_EOL;
+            uv_close($client);
+
+            if (++$this->accepted === 4) {
+                $this->close();
+            }
         });
     }
 
@@ -49,13 +53,8 @@
 $closed = 0;
 for ($i = 0; $i < 4; $i++) {
     $c = uv_tcp_init($loop);
-    uv_tcp_connect($c, uv_ip4_addr('0.0.0.0', 9876), function ($stream, $stat) use (&$closed, $tcpServer) {
-        $closed++;
+    uv_tcp_connect($c, uv_ip4_addr('127.0.0.1', 9876), function ($stream, $stat) {
         uv_close($stream);
-
-        if ($closed === 4) {
-            $tcpServer->close();
-        }
     });
 }
 
