# Config file for /etc/init.d/influxdb

# Various options.
# run `influxd -h` for valid cmdline options
OPTS=""

# Config file
CONFIG="-config /etc/influxdb/influxdb.conf"

# User and group
USER=influxdb
GROUP=influxdb

# Logging
LOG=/var/log/influxdb/influxd.log